LOG_PATH: "logs"

CHINOOK_DB_PATH: "C:/Users/Hp/AppData/Roaming/DBeaverData/workspace6/.metadata/sample-database-sqlite-1/Chinook.db"
CHINOOK_SUMMARIES_DB_PATH: "db/Chinook/sqlite/table_summaries.db"

SQLITE_DB_DIR: "db/Chinook/sqlite"
CHROMA_DB_DIR: "db/Chinook/chromadb"


QUERY_TEXT: "What is the billing city of Leonie Köhler?"
QUERY_TEXT_INITIAL: "LK"


TOP_K: 5
TOP_N: 2
MAX_RETRIES: 3 # attempts to retry failed requests


CHUNK_SIZE: 1000
CHUNK_OVERLAP: 100


MODEL_TEMPERATURE: 0.1
MODEL_REQUEST_TIMEOUT: 480 # seconds
MODEL_CONTEXT_WINDOW: 8000
MODEL_OUTPUT_FORMAT: "json"
MODEL_OUTPUT_FORMAT_JSON: true
MODEL_THINKING: true

USE_LLM_FOR_SUMMARY: true   # set true to use utils.get_llm_func.llm_func for one-line summaries
ROWCOUNT_ESTIMATE_LIMIT: null  # e.g., 100000 for very large tables; null to count all
SUMMARY_STYLE: "one_liner" # "detailed"


LOCAL_EMBEDDING_MODEL: "sentence-transformers/all-MiniLM-L6-v2"
LOCAL_LLM_MODEL: "qwen3:0.6b"